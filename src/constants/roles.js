/**
 * User Roles - Simplified
 * ADMIN: Full access (Doctor/Admin/Staff)
 * PATIENT: Patient portal access only
 */
export const ROLES = {
  ADMIN: 'admin',
  PATIENT: 'patient',
};

export const ALL_ROLES = Object.values(ROLES);
